version: "3.9"

services:
  discord-bot:
    build: .
    container_name: samlam-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=
      - BOT_CLIENT_ID=
      - GUILD_ID=
      - OPENAI_TOKEN=
